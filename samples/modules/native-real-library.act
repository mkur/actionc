; MATH provides portable numerical operations; SYS owns text conversion and I/O.
MODULE NATIVE_REAL_LIBRARY

USE SYS
USE MATH

REAL input, result
STRING text(20)

PROC Main()
  input=2
  MATH.Exp10(@input,@result)
  SYS.StrR(@result,text)
  SYS.PrintRE(@result)
RETURN

ENDMODULE
