# Experiments

This directory is reserved for short-lived local experiments.

Stable compiler regression inputs belong in `fixtures/`. Generated artifacts
belong in `target/`, `/tmp`, or an ignored `outputs/` directory. Longer-lived
research notes belong in `docs/` or `surveys/`.

If an experiment becomes a regular guardrail, move it out of this directory and
update the scripts or tests that run it.
