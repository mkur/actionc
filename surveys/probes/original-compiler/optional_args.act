; Force original compiler code origin to $3000.
SET $491=$3000
SET $E=$3000

MODULE

BYTE seen1, seen2, seen3

PROC Take(BYTE a,b,c)
  seen1 = a
  seen2 = b
  seen3 = c
RETURN

PROC Main()
  Take(1)
  Take(2,3)
RETURN
