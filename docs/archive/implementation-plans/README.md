# Implementation Plan Archive

These plans are retained as historical context, not as the current project
queue. Many slices are complete, superseded, or partially folded into newer
architecture docs.

Use this archive when you need to understand why a mechanism exists or recover
details from an old debugging session. For current work, start with
`../../README.md` and `../../BACKLOG.md`.
