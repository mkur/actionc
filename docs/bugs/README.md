# Bug Notes

This folder keeps focused bug writeups that are still useful for regression
work or understanding fragile compiler behavior.

Use this folder for narrow problem statements with evidence, expected behavior,
and the owning subsystem. Broader execution plans should go in the backlog or,
once stale, under `../archive/implementation-plans/`.
