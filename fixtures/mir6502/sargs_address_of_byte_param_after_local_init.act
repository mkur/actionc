BYTE observed

PROC Noise()
  CHAR ARRAY text(0)="ABCDEFGHIJKLMNOP"
RETURN

PROC Touch(BYTE POINTER p)
  p^ = 7
RETURN

PROC Caller(BYTE a,b,c,d)
  Touch(@c)
  observed = c
RETURN

PROC Main()
  Noise()
  Caller(1,2,3,4)
RETURN
