BYTE i, done, out
BYTE ARRAY data(4) = [0 0 1 0]

BYTE FUNC Stop(BYTE n)
  IF n >= 3 THEN
    RETURN (1)
  FI
RETURN (0)

PROC Main()
  i = 0
  done = 0
  DO
    out ==+ 1
    i ==+ 1
  UNTIL Stop(i) # 0 OR data(i) = 1 OR done # 0
  OD
RETURN
