; Traditional resident names are runtime-neutral. Standalone mode selects only
; the referenced SYSBLK routine from the compiler's embedded runtime image.

BYTE ARRAY buffer(8)=$0600

PROC Main()
  buffer(0)=1
  buffer(7)=2
  Zero(buffer,8)
  DO
  OD
RETURN
