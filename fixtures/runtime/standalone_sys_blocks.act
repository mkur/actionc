; Exercise the remaining initial SYS memory bindings without a cartridge.
MODULE RUNTIME.SYS.BLOCKS

IMPORT SYS.*

BYTE ARRAY source(4)=$0600
BYTE ARRAY destination(4)=$0610

PROC Main()
  SetBlock(source,4,$5A)
  MoveBlock(destination,source,4)
  DO
  OD
RETURN

ENDMODULE
