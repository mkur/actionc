; Portable state and entry for symbol-aware native execution tests.
BYTE command,result
LONGINT itersI,minI,maxI,itersA,minA,maxA
LONGCARD ARRAY values(11),input(11)

INCLUDE "kernel.inc"

PROC Main()
  IF command=0 THEN Init()
  ELSE Initialize(input) FI
  Sort()
  result=CheckResult()
RETURN
